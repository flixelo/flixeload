name: flixeload_sdk
description: FlixeloAd SDK - A Flutter plugin for integrating FlixeloAd mobile advertising network. Display banner, interstitial, native, and rewarded ads.
version: 1.0.2
homepage: https://flixeload.com
repository: https://github.com/flixelo/flixeload
issue_tracker: https://github.com/flixelo/flixeload/issues

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.1.0
  shared_preferences: ^2.2.2
  uuid: ^4.2.1
  cached_network_image: ^3.3.1
  url_launcher: ^6.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
